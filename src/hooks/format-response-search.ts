import { FilterDetails } from '../store/ducks/companies/types';

// export const useFormatResponseSearch = (value: FilterDetails) => {
//     const currentValue = `page=${page}&limit=${limit}`;
//     if(currentValue.)
// };
